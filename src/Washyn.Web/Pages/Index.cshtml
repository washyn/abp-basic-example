@page
@using Microsoft.AspNetCore.Mvc.Localization
@using Volo.Abp.AspNetCore.Mvc.UI.Bootstrap.TagHelpers.Alert
@using Volo.Abp.AspNetCore.Mvc.UI.Bootstrap.TagHelpers.Blockquote
@using Volo.Abp.AspNetCore.Mvc.UI.Bootstrap.TagHelpers.Card
@using Washyn.Domain.Localization
@model Washyn.Web.Pages.Index
@inject IHtmlLocalizer<WashinResource> L
@{
    // Layout = null;
}

<h1>Welcome to example abp basic solution with 4 layers.</h1>
<hr>

<h5 class="text-center">TimeZones for country</h5>

<ul>
    @foreach (var item in Model.TimeZones)
    {
        <li>
            @item.Key - @item.Value
        </li>
    }
</ul>

<h5 class="text-center">TimeZones ids </h5>
@foreach (var item in Model.TimeZoneIds)
{
    <li>
        @item
    </li>
}


<h2>Windows Format is : @Model.WindowsFormat</h2>

<hr>

<h5 class="text-center">test</h5>

<abp-card>
    <abp-card-header>
        <abp-card-title>
            Test title
        </abp-card-title>
    </abp-card-header>
    <abp-card-body>
        Lorem ipsum es el texto que se usa habitualmente en diseño gráfico en demostraciones de tipografías o de borradores de diseño para probar el diseño visual antes de insertar el texto final.
        
        <br>
        
        <strong>Example localization: </strong>
        <p>
            @(L["KeyLocalizationExample"])
        </p>
        
        
        <hr>
        
        <div>
            @if (HttpContext.User.Identity != null && HttpContext.User.Identity.IsAuthenticated)
            {
                <a href="/logout" class="btn btn-outline-secondary">Logout</a>
            }
            else
            {
                <a href="/login" class="btn btn-outline-secondary">Login</a>
            }
        </div>
        
        <hr>
        <h3>Claims</h3>
        <ul>
            @foreach (var iten in HttpContext.User.Claims)
            {
                <li><strong>@iten.Type : </strong> @iten.Value </li>
            }
        </ul>
        
        
        <abp-row>
            <abp-column>
                <abp-card>
                    <abp-card-header>
                        <abp-card-title>
                            CustomCurrentUser
                        </abp-card-title>
                    </abp-card-header>
                    <abp-card-body>
                        <ul>
                            <li><strong> Model.CustomCurrentUser.IsAuthenticated : </strong> @Model.CustomCurrentUser.IsAuthenticated</li>
                            <li><strong> Model.CustomCurrentUser.Name : </strong> @Model.CustomCurrentUser.Name</li>
                            <li><strong> Model.CustomCurrentUser.SurName : </strong> @Model.CustomCurrentUser.SurName</li>
                            <li><strong> Model.CustomCurrentUser.Email : </strong> @Model.CustomCurrentUser.Email</li>
                            <li><strong> Model.CustomCurrentUser.Roles : </strong> @Model.CustomCurrentUser.Roles.Any()</li>
                            <li><strong> Model.CustomCurrentUser.UserName : </strong> @Model.CustomCurrentUser.UserName</li>
                            <li><strong> Model.CustomCurrentUser.Id : </strong> @Model.CustomCurrentUser.Id</li>
                        </ul>
                    </abp-card-body>
                </abp-card>
            </abp-column>
            <abp-column>
                <abp-card>
                    <abp-card-header>
                        <abp-card-title>
                            CurrentUser
                        </abp-card-title>
                    </abp-card-header>
                    <abp-card-body>
                        <ul>
                            <li><strong> Model.CurrentUser.IsAuthenticated : </strong> @Model.CurrentUser.IsAuthenticated</li>
                            <li><strong> Model.CurrentUser.Name : </strong> @Model.CurrentUser.Name</li>
                            <li><strong> Model.CurrentUser.SurName : </strong> @Model.CurrentUser.SurName</li>
                            <li><strong> Model.CurrentUser.Email : </strong> @Model.CurrentUser.Email</li>
                            <li><strong> Model.CurrentUser.Roles : </strong> @Model.CurrentUser.Roles.Any()</li>
                            <li><strong> Model.CurrentUser.UserName : </strong> @Model.CurrentUser.UserName</li>
                            <li><strong> Model.CurrentUser.Id : </strong> @Model.CurrentUser.Id</li>
                        </ul>
                    </abp-card-body>
                </abp-card>
            </abp-column>
        </abp-row>
        
        
    </abp-card-body>
    <abp-card-footer>
        test footer
    </abp-card-footer>
</abp-card>